---
import type { FooterLink } from "@/types";
import { cn } from "@/utils/cn";
import { Icon } from "astro-icon/components";
import CustomLink from "./CustomLink.astro";
import { buttonVariants } from "./ui/button";

const links: Array<FooterLink> = [
  {
    url: "/",
    description: "VECTOR's official website",
    icon: "mdi:home",
  },
  {
    url: "https://www.linkedin.com/company/vectoraico/",
    description: "VECTOR on LinkedIn",
    icon: "fa-brands:linkedin",
  },
  {
    url: "https://www.instagram.com/vectoraico/",
    description: "VECTOR on Instagram",
    icon: "fa-brands:instagram",
  },
  {
    url: "mailto:hello@vector-ai.co",
    description: "VECTOR's email address",
    icon: "mdi:email",
  },
];

const navItems = [
  {
    name: "Home",
    link: "/",
  },
  {
    name: "Team",
    link: "/team",
  },
  {
    name: "vFit",
    link: "/products/vfit",
  },
];
---

<footer class="relative z-20 bg-white dark:bg-black dark:text-white">
  <div
    class="mx-auto w-full max-w-screen-xl p-4 text-gray-500 dark:text-gray-200 md:py-8"
  >
    <div class="sm:flex sm:items-center sm:justify-between">
      <a
        href="/"
        class="mb-4 flex items-center space-x-3 invert dark:invert-0 sm:mb-0"
      >
        <img src="/vector.png" class="h-8" alt="VECTOR Logo" />
      </a>

      <ul
        class="mb-6 flex flex-wrap items-center gap-4 text-sm font-medium text-gray-500 dark:text-gray-300 sm:mb-0"
      >
        {
          links.map((link) => (
            <li>
              <CustomLink
                class="me-6 transition-all hover:text-black hover:underline dark:hover:text-white"
                href={link.url}
                rel="noreferrer"
                target="_blank"
              >
                <span class="sr-only">{link.description}</span>
                <Icon class="size-full" name={link.icon} />
              </CustomLink>
            </li>
          ))
        }
      </ul>
    </div>

    <hr class="my-6 border-gray-500 dark:border-gray-200 sm:mx-auto lg:my-8" />

    <div
      class="flex justify-between max-sm:flex-wrap-reverse max-sm:text-center"
    >
      <div
        class="flex w-full flex-wrap items-start justify-center *:w-full sm:w-2/5 sm:justify-start"
      >
        <h4 class="font-medium">Headquarters</h4>
        <a
          class="w-fit text-balance hover:underline"
          href="https://maps.app.goo.gl/jXaC62RkZwX3LLAk7"
          target="_blank"
          rel="noreferrer"
        >
          Office No. 21/22, 2nd Floor, Al Hameed Mall, G 11 Markaz,
          Islamabad Capital Territory
        </a>
      </div>

      <div
        class="flex w-full flex-col flex-wrap items-start justify-center *:w-full max-sm:my-4 sm:w-2/5 sm:text-right"
      >
        <h4 class="font-medium">Founded</h4>
        <p>June, 2023</p>
      </div>
    </div>

    <hr class="my-6 border-gray-500 dark:border-gray-200 sm:mx-auto lg:my-8" />

    <div class="flex justify-between *:w-full *:items-center max-sm:flex-wrap">
      <div class="flex justify-center max-md:my-2 md:justify-start">
        <span class="block text-sm sm:text-center"
          >© {new Date().getFullYear()}
          <a href="/" class="hover:underline">VECTOR</a>. All Rights
          Reserved.
        </span>
      </div>

      <div class="flex justify-center max-md:my-2 md:justify-center">
        {
          navItems.map((item) => (
            <CustomLink
              href={item.link}
              class={cn(
                buttonVariants({ variant: "link" }),
                "text-gray-500 dark:text-gray-200 ",
              )}
            >
              {item.name}
            </CustomLink>
          ))
        }
      </div>

    
    </div>
  </div>
</footer>
